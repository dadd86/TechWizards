# Carpeta `SQL`

Contiene el archivo `PrimerSQL.sql` con el diseño inicial de la base de datos (DDL) para Tech Wizards. Sirve como referencia para validar que las entidades Room (`data.local.entity`) coinciden con el esquema relacional esperado.

Usar esta carpeta para almacenar scripts de migración manual, análisis de consultas o fixtures reproducibles.