# Paquete `data.infra`

Abstrae infraestructura adicional para la capa de datos.

- `di/`: punto de extensión para módulos de inyección de dependencias o wiring manual complementario al `ServiceLocator`.

Este paquete actúa como borde anti-corrupción para adaptadores externos que no pertenecen a Room ni DataStore.