package com.diegodiaz.techwizards.domain.model

/**
 * Representa el resultado de una partida.
 * Se usa a nivel de dominio (y puede mapearse a Entity).
 */
enum class Resultado {
    VICTORIA,
    DERROTA
}
