package com.diegodiaz.techwizards.domain.repository

/**
 * ChatRepository.kt
 *
 * Este repositorio manejará los mensajes del chat (para el modo multijugador).
 * No se utiliza en el Producto 1, pero se deja definido para escalabilidad.
 */
interface ChatRepository {
    // suspend fun enviarMensaje(usuarioId: Int, mensaje: String)
    // suspend fun obtenerMensajes(matchId: Int): List<Mensaje>
}
