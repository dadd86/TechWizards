package com.diegodiaz.techwizards.domain.repository

/**
 * EventoRepository.kt
 *
 * Gestiona eventos o sucesos especiales (por ejemplo, logros o misiones).
 * De momento lo dejamos vacío y preparado para futuras ampliaciones.
 */
interface EventoRepository {
    // suspend fun registrarEvento(tipo: String, descripcion: String)
}
