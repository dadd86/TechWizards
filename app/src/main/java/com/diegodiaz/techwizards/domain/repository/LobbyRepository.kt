package com.diegodiaz.techwizards.domain.repository

/**
 * LobbyRepository.kt
 *
 * Controla las funciones relacionadas con los lobbies (salas de espera o conexión).
 * En el Producto 1 no se usa todavía, pero se deja listo para expansiones (Producto 3).
 */
interface LobbyRepository {
    // suspend fun crearLobby(nombre: String)
    // suspend fun obtenerLobbies(): List<Lobby>
}
