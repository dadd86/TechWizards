# `util.time`

Espacio para utilidades de fecha/hora compartidas. `DateFormats` será el contenedor de patrones `java.time` reutilizables (por ejemplo `dd MMM yyyy`, `HH:mm`).

Al implementar, exponer funciones puras que no dependan de Android para facilitar pruebas unitarias.